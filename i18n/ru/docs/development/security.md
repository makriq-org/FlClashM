# 🔒 Политика безопасности

Ключевой инвариант форка: **оформление и подсказки провайдера не могут ослаблять защиты среды выполнения.**

## 🛡 Runtime-политика

`SecurityPolicy` принудительно включает TUN на Android. Остальные параметры профиля и runtime-конфигурации этот слой не меняет.

## 📱 Защиты Android

- 🔌 TUN принудительно включается.
- 🎯 Раздельное туннелирование из профиля имеет приоритет.
- 📸 Индикатор применённых пакетных правил читает **снимок Android VPN-службы**, а не текущую конфигурацию ядра (см. [снимок VPN](runtime.md#-снимок-применённых-правил-android-vpn)).
- ✍️ Каталог обновлений приложения проверяется встроенным открытым ключом **Ed25519** до разбора содержимого.
- 🔐 Загрузчик обновлений проверяет **SHA-256** APK из подписанного каталога и перебирает только перечисленные в нём зеркала.
- 🧾 Подпись APK дополнительно проверяется установщиком Android относительно подписи уже установленного приложения.
- 🚫 Встроенные узлы не могут задавать локальные адреса и порты.
- 📞 `olcrtc` работает только в режиме CNC.
- 🧱 OlcRTC отклоняет `profiles`, `failover`, `video.hw`, локальные listener и ключи из файлов.
- 🛡 `byedpi` проверяет только указанные URL.
- 🔗 Удалённые стратегии ByeDPI используют те же правила публичного HTTPS, лимитов и stale-кэша, что и списки StormDNS.
- 🌩 `stormdns` дополнительно не принимает SOCKS5-аутентификацию, блок `local-dns-*`, каталог журнала, `config-version` и интерактивный `startup.mode: ask`.

## 🌩 StormDNS

⚠️ `encryption: none` и `xor` разрешены по контракту upstream: они **не скрывают содержимое** от оператора резольвера.

`resolvers` — единственный случай, когда профиль передаёт узлу список сетевых адресов, поэтому источники проверяются отдельно:

- ✅ Разрешённые источники в `resolvers`: `system`, IP, `IP:port`, CIDR-диапазон и `https://`-ссылка на список.
- 🔗 Ссылка на список принимается **только по HTTPS**, без userinfo и fragment, без `localhost` и локальных адресов; ответ ограничен **1 MiB** и **15 с**.
- 🚫 Загрузка списка не идёт через прокси, не следует за редиректами, принимает только ответ `200` и соединяется только с уже проверенным публичным адресом.
- 💾 Кэш удалённого списка ведётся отдельно на каждую ссылку. При недоступности источника используется последняя копия — даже после истечения `refresh`; без копии ссылка просто пропускается, а не роняет применение профиля.
- 🧮 Раскрытие CIDR ограничено 65536 адресами: upstream на широких диапазонах итерирует их все, что повесило бы приложение.
- 🔁 Итоговый список дедуплицируется по IP — побеждает первое вхождение вместе со своим портом.

## 🌐 Адреса проверки доступности

Для `connectivity-check` и `strategy-test` разрешены **только** HTTP(S)-адреса без учётных данных и локальных назначений.

- 🚫 Запрещены петлевые, частные, канальные, многоадресные и служебные диапазоны, а также локальные имена.
- 🔁 Перед сетевым запросом результат DNS проверяется **повторно**.
- 🔗 Соединение к назначению устанавливается только командой SOCKS через проверяемый встроенный узел.

## 🎨 Заголовки провайдера

- 💡 Заголовки `flclashm-*` остаются **рекомендательными** (см. [подсказки провайдера](../user-guide/provider-hints.md)).
- 🔒 Оформление **не может** менять защиты среды выполнения.

---

> 🌍 Другие языки: [English](../../../en/docs/development/security.md) · [中文](../../../zh/docs/development/security.md) · [فارسی](../../../fa/docs/development/security.md)
