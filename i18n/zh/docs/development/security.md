# 安全策略

## 核心原则

安全策略由客户端决定，而不是提供商。

**提供商可以：**
- 传递元数据；
- 外观提示；
- 建议性设置。

**提供商不能：**
- 削弱客户端安全性；
- 通过响应头强制执行行为。

## Android 安全

- TUN 强制开启
- 更新加载器验证 SHA256 校验和
- 内置节点不能设置本地地址和端口
- `olcrtc` 仅在 CNC 模式下工作
- `byedpi` 仅测试指定的 URL

## 提供商响应头

- `flclashm-*` 响应头仅供参考
- 外观不能改变运行时安全性
